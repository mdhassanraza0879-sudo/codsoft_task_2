{
  "name": "dinedesk-platform",
  "version": "1.0.0",
  "description": "DineDesk – Restaurant Ordering & Management Platform (CodSoft Full Stack Task 2)",
  "scripts": {
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"blue,magenta\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "seed": "npm run prisma:seed --prefix backend",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend"
  },
  "keywords": [
    "restaurant",
    "ordering",
    "management",
    "nextjs",
    "express",
    "postgresql",
    "prisma",
    "typescript",
    "codsoft"
  ],
  "author": "CodSoft Full Stack Intern",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
